# Optional production URL overrides. Leave unset for local `npm run dev`.
# SITE is the public origin (no trailing path).
# SITE=https://cato149.github.io
# BASE_PATH is the folder GitHub Pages mounts a project site under.
# Use / for a user site (username.github.io) or a custom domain.
# BASE_PATH=/varectra-landing/
